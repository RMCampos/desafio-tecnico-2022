import React from 'react';

const Item = () => {
  /*
  if (searchTerm === null) {
    return (
      <div>
        <h2>{searchTerm} Images</h2>
        <Container searchTerm={searchTerm} />
      </div>
    );
  }

  return (
    <div className="container">
      <Header />
      <h2>Fill the form to search!</h2>
    </div>
  );
  */
 return (
  <p>Item</p>
 );
};

export default Item;
